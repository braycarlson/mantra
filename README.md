# mantra

A library for audio endpoint control on Windows and Linux. It enumerates capture and render devices,
reads and writes their mute state and volume, moves the default device, and reports device changes
as they happen.
